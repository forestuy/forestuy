{
  "name": "forest-admin",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts-check.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@netlify/blobs": "11.1.0"
  }
}
